# FlowState Frontend

The user interface for the FlowState Task & Energy Manager.

## 🚀 Getting Started

1.  **Install dependencies**:
    ```bash
    npm install
    ```

2.  **Start development server**:
    ```bash
    npm run dev
    ```

3.  **Build for production**:
    ```bash
    npm run build
    ```

## 🏗️ Architecture

-   `src/components`: Reusable UI modules (Energy panel, Task queue, etc.)
-   `src/pages`: Main application views.
-   `src/App.jsx`: Main routing and layout wrapper.
-   `src/index.css`: Global styles and Tailwind configuration.

## 🛠️ Key Technologies

-   **React 19**
-   **Vite**
-   **Tailwind CSS**
-   **Lucide React** (Icons)
-   **Recharts** (Data Visualization)
